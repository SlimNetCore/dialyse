spring:
  datasource:
    url: jdbc:h2:mem:hemodialyse;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never

app:
  security:
    jwt:
      secret: test-secret-test-secret-test-secret


# Infra - Sprint 1 bootstrap

Ce dossier contient les assets d infrastructure (docker, ci/cd, scripts).

## Etat actuel
- `docker-compose.yml` a la racine avec service PostgreSQL.

## Prochaine etape
- Ajouter Dockerfiles front/back.
- Ajouter pipelines `Jenkinsfile`, `.gitlab-ci.yml`, `.github/workflows/ci.yml`.

